// dsh-toolkit: Node-side re-export of the dsh-skill-manager client half.
// (The browser aggregate client lives at ./client; this subpath exists so the
// exports map stays complete for tools that import the member client directly.)
export { default } from '@wanghailong0419/dsh-skill-manager/client';
export * from '@wanghailong0419/dsh-skill-manager/client';
